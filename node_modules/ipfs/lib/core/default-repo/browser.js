'use strict';

var Store = require('idb-pull-blob-store');
var IPFSRepo = require('ipfs-repo');

module.exports = function (dir) {
  var repoPath = dir || 'ipfs';
  return new IPFSRepo(repoPath, { stores: Store });
};