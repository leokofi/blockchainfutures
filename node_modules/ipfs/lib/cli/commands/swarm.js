'use strict';

module.exports = {
  command: 'swarm',

  description: 'Swarm inspection tool.',

  builder: function builder(yargs) {
    return yargs.commandDir('swarm');
  },
  handler: function handler(argv) {}
};