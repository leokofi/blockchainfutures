'use strict';

module.exports = {
  command: 'bootstrap',

  description: 'Show or edit the list of bootstrap peers.',

  builder: function builder(yargs) {
    return yargs.commandDir('bootstrap');
  },
  handler: function handler(argv) {}
};