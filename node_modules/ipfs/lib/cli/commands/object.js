'use strict';

module.exports = {
  command: 'object',

  description: 'Interact with ipfs objects.',

  builder: function builder(yargs) {
    return yargs.commandDir('object');
  },
  handler: function handler(argv) {}
};